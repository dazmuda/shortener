<h1>Add a new URL</h1>
<%= form_for @shortened_url do |form| %>

  <p>
    <%= form.label :url, "Your URL:" %>
    <%= form.text_field :url %>
  </p>

  <p class='buttons'>
    <%= form.submit "Shorten my URL" %>
  </p>

<% end %>